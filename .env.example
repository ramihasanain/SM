# Copy to .env and fill in real values. Never commit .env to git.

SECRET_KEY=
DEBUG=True

# PostgreSQL on DigitalOcean (optional locally — defaults to sqlite)
DATABASE_URL=

FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
FACEBOOK_REDIRECT_URI=

GEMINI_API_KEY=
# Legacy name still supported if not renamed on the server yet:
# ai_key=
# Optional — defaults match current Google AI model IDs (Jun 2026):
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MODEL_FALLBACKS=gemini-2.5-flash-lite,gemini-flash-latest

TOTP_ISSUER=Analytica
